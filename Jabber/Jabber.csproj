<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>org.GraphDefined.Vanaheimr.Ratatoskr</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!--
      Nur noch das Protokoll der Ausgabe: Die Konsole schreibt selbst und
      braucht dafuer einen Logger. BouncyCastle stand hier, solange OMEMO hier
      lag - das tut es seit D97 nicht mehr, und ein Paket, das niemand mehr
      benutzt, ist eine Behauptung ueber eine Abhaengigkeit, die es nicht gibt.
    -->
    <PackageReference Include="Microsoft.Extensions.Logging" Version="10.0.10" />
    <PackageReference Include="Microsoft.Extensions.Logging.Console" Version="10.0.9" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\libs\Ratatoskr\Ratatoskr\Ratatoskr.csproj" />
  </ItemGroup>

</Project>
